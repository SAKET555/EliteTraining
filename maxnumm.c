#include<stdio.h>
#include<stdlib.h>

int main()
{
    int i,n;
    int* arr[] = (int*)malloc(100 * sizeof(int));
    int* array[] = (int*)malloc(100*sizeof(int));
    for(i=0;i<n;i++)
    {
        scanf("%d\n",&arr[i]);
    }
    for(i=n;i>=0;i--)
    {
        array[]

    }
    return 0;
}